# action.yml
name: 'aziona'

description: 'Action aziona-cli docker'

inputs:
  target:
    description: 'Target'
    required: true

outputs:
  result:
    description: 'Build result'

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.target }}